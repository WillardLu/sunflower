# http server配置参数
[server]
# 监听的地址
address = "192.168.0.104"
# 监听的端口
port = "8080"
# 读取请求头所允许的时间长度。以下时间相关参数在程序中都会处理成秒。
ReadHeaderTimeout = 20
# 读取整个请求（包括正文）的最长持续时间
ReadTimeout = 60
# 指从读取请求开始，服务器在写入响应之前等待的最长时间。
# 可以将其视为处理程序响应请求的时间。
WriteTimeout = 120
# 启用保持连接时等待下一个请求的最长时间
IdleTimeout = 30